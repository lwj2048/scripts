name: test

on:
  workflow_dispatch:
  
jobs:
  setup-proxy:
    runs-on: ubuntu-latest

    steps:
      - name: Set up python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: 3.13
      - name: Install lazyllm
        shell: bash
        run: |
          which python
          which pip
          python -V
          pip install lazyllm
      
